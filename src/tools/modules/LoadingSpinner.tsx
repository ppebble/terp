import * as React from 'react';
import { Oval } from 'react-loader-spinner';

function LoadingSpinner() {
  return <Oval color="#4d66ba" height={100} width={100} />;
}

export default LoadingSpinner;
